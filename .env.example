# Example environment file — DO NOT commit secrets to git
# Copy this to /etc/myapp.env or project root as `.env` and set real values.

DB_USER=your_db_user
DB_PASSWORD=your_db_password_here
DB_HOST=your_db_host_or_endpoint
DB_PORT=3306
DB_NAME=your_database_name

# AWS settings (optional)
AWS_REGION=us-east-1
AWS_PROFILE=

# If you prefer Secrets Manager instead, put the secret name here and fetch at runtime
SECRET_NAME=my/db/credentials
